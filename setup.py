from setuptools import setup


setup(name='mvd-rl',
      version='0.1.1',
      description='Measure-Valued Derivatives for Reinforcement Learning',
      license='MIT',
      packages=['src', 'scripts'],
      zip_safe=False,
      install_requires=[],
      )
